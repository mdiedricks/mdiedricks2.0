import React from 'react'
import { connect } from 'frontity'

const Contact = ({state}) => {
    return(
        <div>
            Contact page
        </div>
    )
}

export default connect(Contact)