import React from 'react'
import { connect, styled, css } from 'frontity'
import Link from './link';

 function projectCard(props) {
    return (
        <div>
            <span>This is a an project card</span>
        </div>
    )
}

export default connect(projectCard)